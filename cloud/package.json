{
  "name": "interlinked-cloud",
  "version": "0.0.1",
  "private": true,
  "description": "Interlinked cloud governor — observes agent activity at the cloud tier and returns runner-compatible verdicts (block/warn/allow)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.95.0"
  }
}
