# Auto-generated reference docs

These files are generated from live harness data by `scripts/generate-docs.ts`.
Regenerate anytime with:

```bash
npm run docs
```

The command reference (`cli-reference.md`) is regenerated per-checkout
because it bakes in the current working directory as the `--cwd` default.
Run `npm run docs` after cloning to produce it locally.
