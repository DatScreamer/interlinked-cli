# no-tests fixture

A tiny dependency-free Node CLI used by the harness-compat evals. No test
runner is configured on purpose — this shape checks the harness against repos
without a test culture.

## Usage

```sh
node index.js greet Ada
node index.js slug "Hello World"
```
