// =========================================================
// Protocol v3 — compiled contract identity
// =========================================================

/**
 * Digest of the frozen protocol-v3 artifacts and normative implementation.
 *
 * This module is deliberately NOT one of the digest inputs: including the
 * digest in its own preimage would be self-referential. The schema-conformance
 * suite recomputes the contract digest and requires this compiled value and
 * `contract-digest.json` to agree.
 */
export const PROTOCOL_V3_CONTRACT_DIGEST =
	"cfa90530c734df5c0fe6e3b00494bf259c48d786954e0a561e53e8cb9a6cd420";
