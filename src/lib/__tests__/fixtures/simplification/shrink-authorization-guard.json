{
  "schema_version": "simplification-adversarial/v1",
  "fixture_id": "shrink-authorization-guard",
  "remedy": "shrink",
  "trap_kind": "protected-behavior",
  "repository_files": [
    {
      "path": "src/delete-project.ts",
      "language": "typescript",
      "content": "const project = await loadProject(id);\nif (project.tenantId !== actor.tenantId) throw new ForbiddenError();\nawait deleteProject(project);"
    },
    {
      "path": "test/delete-project.test.ts",
      "language": "typescript",
      "content": "it('refuses cross-tenant deletion', async () => { /* authorization contract */ });"
    }
  ],
  "candidate": {
    "summary": "Shrink the handler by delegating directly to deleteProject",
    "replacement": "return deleteProject(await loadProject(id))",
    "claimed_evidence": [
      "delegating-wrapper"
    ]
  },
  "protected_boundaries": [
    "authorization",
    "data-loss-prevention"
  ],
  "required_read_paths": [
    "src/delete-project.ts",
    "test/delete-project.test.ts"
  ],
  "expected": {
    "disposition": "reject",
    "patch_eligible": false,
    "reason_codes": [
      "authorization-check-is-not-wrapper-noise",
      "deletion-needs-tenant-boundary"
    ]
  }
}
